# 实验四：毕昇编译器验证实验 —— 一键执行手册

> **当前状态**：所有源文件（`.c`）、CoreMark 源码、目录结构均已在 `D:/Desktop/bex4/bisheng_lab/` 下就绪，你**不需要再写任何代码**。
>
> **你的任务**：启动 Docker 容器，依次复制粘贴以下命令，运行、截图、填入报告。

---

## 第一步：进入 Docker 容器（只需执行一次）

在 **WSL 终端** 里执行：

```bash
docker run -it --rm --platform linux/arm64 \
  -v "/mnt/d/Desktop/bex4:/home/student/workspace" \
  rongzhaokang/bisheng-lab:latest
```

进入容器后，提示符应为 `[student@xxxx]$`，表示你已处于 `linux/arm64` 实验环境。

---

## 第二步：逐个实验执行命令

以下命令按实验顺序排列。每条都可以**直接复制粘贴**到容器中执行。

---

### 实验 0：环境与基线（验证常量折叠与分支消除）

```bash
cd ~/workspace/bisheng_lab/exp0_baseline

# 1. 编译运行
clang -O0 toy_opt.c -o toy_O0 && ./toy_O0
clang -O2 toy_opt.c -o toy_O2 && ./toy_O2

# 2. 生成汇编
clang -O0 -S toy_opt.c -o toy_O0.s
clang -O2 -S toy_opt.c -o toy_O2.s

# 3. 对比汇编差异（重点：bar() 函数）
diff -u toy_O0.s toy_O2.s | sed -n '1,120p'

# 4. 计时（各跑 3 次）
echo "=== O0 x3 ==="
for i in 1 2 3; do time -p ./toy_O0; done

echo "=== O2 x3 ==="
for i in 1 2 3; do time -p ./toy_O2; done
```

**你需要截图的内容**：
- O0 与 O2 的运行输出（都是 `101`）
- `diff -u` 输出的 `bar()` 函数差异（O0 版完整分支 vs O2 版仅剩 `mov w0, #101`）
- `time -p` 各 3 次的结果

---

### 实验 1：自动并行化（向量化 vs OpenMP）

```bash
cd ~/workspace/bisheng_lab/exp1_parallel

# 1. 编译三个版本
clang -O2 vec_add.c -o vec_no_vec
clang -O2 vec_add_vec.c -o vec_vec
clang -O2 -fopenmp vec_add_omp.c -o vec_omp

# 2. 运行计时（各 3 次）
echo "=== no-vec x3 ==="
for i in 1 2 3; do time -p ./vec_no_vec; done

echo "=== vec x3 ==="
for i in 1 2 3; do time -p ./vec_vec; done

echo "=== omp x3 ==="
for i in 1 2 3; do time -p ./vec_omp; done

# 3. 生成汇编
clang -O2 -S vec_add.c -o vec_no_vec.s
clang -O2 -S vec_add_vec.c -o vec_vec.s

# 4. 提取向量化证据
echo "=== vec 版本向量指令 ==="
grep -nE 'ldr\s+q|fadd\s+v|stur\s+q' vec_vec.s | head -n 20

echo "=== no-vec 版本向量指令 ==="
grep -nE 'ldr\s+q|fadd\s+v|stur\s+q' vec_no_vec.s | head -n 20
```

**你需要截图的内容**：
- 三个版本的运行时间（记录中位数）
- `vec_vec.s` 中出现 `fadd v0.4s`（NEON 向量指令）
- `vec_no_vec.s` 中无向量指令（对比说明）

---

### 实验 2：内存布局优化（AoS vs SoA）

```bash
cd ~/workspace/bisheng_lab/exp2_memlayout

# A 部分：struct-peel（可选，只编译不运行）
clang struct_peel.c -O3 -flto -fuse-ld=lld -o test.struct-peel.exe
objdump -d test.struct-peel.exe > test.struct-peel.dump
grep -cE 'bl.*(calloc|malloc)' test.struct-peel.dump

# B 部分：AoS -> SoA（必做）
clang -O3 aos_soa.c -o aos_soa

# 运行 3 次
echo "=== AoS/SoA x3 ==="
for i in 1 2 3; do time -p ./aos_soa; done

# 生成汇编并提取证据
clang -O3 -S aos_soa.c -o aos_soa_O3.s

echo "=== AoS: 标量跨步访存 ldr s ==="
grep -nE 'ldr\s+s|fadd\s+s' aos_soa_O3.s | head -n 20

echo "=== SoA: 向量连续访存 ldp q ==="
grep -nE 'ldr\s+q|ldp\s+q|fadd\s+v' aos_soa_O3.s | head -n 20
```

**你需要截图的内容**：
- `checksum_aos` 与 `checksum_soa` 一致
- `time_aos` 与 `time_soa` 的中位数对比
- `ldr s1, [x11], #128`（AoS 跨步）vs `ldp q1, q4, [x11, #-16]`（SoA 向量）

---

### 实验 3：PGO 反馈优化

```bash
cd ~/workspace/bisheng_lab/exp3_pgo

# 1. 基线编译运行
clang -O2 bubble_sort.c -o base.exe

echo "=== 基线 x3 ==="
for i in 1 2 3; do time -p ./base.exe; done

# 2. 插桩编译 + 收集 profile
clang -O2 -fprofile-instr-generate bubble_sort.c -o sample.exe
LLVM_PROFILE_FILE="code-%p.profraw" ./sample.exe >/dev/null

# 3. 合并 profile
llvm-profdata merge -output=code.profdata code-*.profraw

# 4. 反馈编译运行
clang -O2 -fprofile-instr-use=code.profdata bubble_sort.c -o pgo.exe

echo "=== PGO x3 ==="
for i in 1 2 3; do time -p ./pgo.exe; done

# 5. 反汇编对比
objdump -d base.exe > base.dump
objdump -d pgo.exe > pgo.dump

echo "=== base: bubble_sort ==="
grep -nA80 '<bubble_sort>:' base.dump | head -n 85

echo "=== pgo: bubble_sort ==="
grep -nA80 '<bubble_sort>:' pgo.dump | head -n 85
```

**你需要截图的内容**：
- 基线 `Average: xxx ms` vs PGO `Average: xxx ms`
- `grep -nA80` 输出的 `bubble_sort` 附近反汇编对比（PGO 版通常更紧凑）

---

### 实验 4：Autotuner（CoreMark 调优）

```bash
cd ~/workspace/bisheng_lab/exp4_autotuner_coremark/coremark

# 直接执行已准备好的脚本（耗时较长，约 5~10 分钟）
bash run.sh 2>&1 | tee autotuner_log.txt
```

**执行完成后**，验证 config.yaml：

```bash
ls -lh /tmp/autotuner_data/config.yaml
cat /tmp/autotuner_data/config.yaml | head -n 30
```

**你需要截图的内容**：
- `run.sh` 执行过程中的 10 轮迭代输出（至少截迭代过程和最终对比）
- O2 基线 vs Autotuned 的 `Total time` / `Iterations/Sec`
- `config.yaml` 已生成的证明

---

### 实验 5：综合循环体优化（Loop Fusion + Autotuner 对比）

```bash
cd ~/workspace/bisheng_lab/exp5_loopopt_project

# 1. 编译各版本
clang -O0 loop_fusion_test.c -o unfused_O0
clang -O1 loop_fusion_test.c -o unfused_O1
clang -O2 loop_fusion_test.c -o unfused_O2
clang -O2 -DFUSED=1 loop_fusion_test.c -o fused_O2

# 2. 运行各 3 次
echo "=== unfused_O0 x3 ==="
./unfused_O0
./unfused_O0
./unfused_O0

echo "=== unfused_O1 x3 ==="
./unfused_O1
./unfused_O1
./unfused_O1

echo "=== unfused_O2 x3 ==="
./unfused_O2
./unfused_O2
./unfused_O2

echo "=== fused_O2 x3 ==="
./fused_O2
./fused_O2
./fused_O2

# 3. 生成汇编
clang -O2 -S loop_fusion_test.c -o unfused_O2.s
clang -O2 -DFUSED=1 -S loop_fusion_test.c -o fused_O2.s

# 4. 统计内循环头数量
echo "=== unfused 内循环头 ==="
grep -n 'Inner Loop Header' unfused_O2.s | head -n 20

echo "=== fused 内循环头 ==="
grep -n 'Inner Loop Header' fused_O2.s | head -n 20
```

**Autotuner 对比（可选）**：

```bash
# Autotuner 调优（约 1~2 分钟）
export AUTOTUNE_DATADIR=/tmp/autotuner_fusion
rm -rf /tmp/autotuner_fusion && mkdir -p /tmp/autotuner_fusion
clang -O2 -o fusion_tuned loop_fusion_test.c -fautotune-generate
llvm-autotune minimize
for i in 1 2 3 4 5; do
  clang -O2 -o fusion_tuned loop_fusion_test.c -fautotune
  t=$(./fusion_tuned | grep 'time=' | awk -F'time=' '{print $2}')
  echo "Iter $i time=$t"
  llvm-autotune feedback "$t"
done
llvm-autotune finalize
clang -O2 -o fusion_tuned loop_fusion_test.c -fautotune

echo "=== fusion_tuned x3 ==="
./fusion_tuned
./fusion_tuned
./fusion_tuned

ls -lh /tmp/autotuner_fusion/config.yaml
```

**你需要截图的内容**：
- 各版本 `Checksum`（应一致）
- O0 / O1 / O2 / fused 的运行时间对比
- `Inner Loop Header` 数量对比（unfused 多，fused 少）
- `config.yaml` 已生成（如做了 Autotuner 部分）

---

## 结果记录表格（供你填入报告）

| 实验 | 版本 | 编译选项 | 时间中位数 | 正确性校验 | 关键汇编证据 |
|------|------|----------|-----------|-----------|-------------|
| 实验0 | O0 | `-O0` | | 输出：`101` | `bar()` 完整分支 |
| 实验0 | O2 | `-O2` | | 输出：`101` | `bar()` 仅剩 `mov w0,#101` |
| 实验1 | no-vec | `-O2` + pragma | | 正常输出 | 无向量指令 |
| 实验1 | vec | `-O2` | | 正常输出 | `fadd v0.4s` |
| 实验1 | omp | `-O2 -fopenmp` | | 正常输出 | 4 线程 |
| 实验2 | AoS | `-O3` | | checksum= | `ldr s1, [x11], #128` |
| 实验2 | SoA | `-O3` | | checksum= | `ldp q1, q4, [x11, #-16]` |
| 实验3 | 基线 | `-O2` | | ms | bubble_sort 布局 |
| 实验3 | PGO | `-O2 + profile` | | ms | bubble_sort 更紧凑 |
| 实验4 | O2 | `-O2` | | iter/sec | 基线 |
| 实验4 | Autotuned | `-fautotune` | | iter/sec | config.yaml 已生成 |
| 实验5 | unfused_O0 | `-O0` | | checksum= | |
| 实验5 | unfused_O1 | `-O1` | | checksum= | |
| 实验5 | unfused_O2 | `-O2` | | checksum= | 8 个 Inner Loop Header |
| 实验5 | fused_O2 | `-O2 -DFUSED` | | checksum= | 6 个 Inner Loop Header |
| 实验5 | tuned | `-fautotune` | | checksum= | config.yaml 已生成 |

---

## 思考题（回答模板）

**实验0**
1. 为什么在 `-O2` 编译下 `10*10` 可能不会在运行时计算？
2. 为什么优化后程序输出仍然必须保持一致？
3. 从汇编角度看，优化后代码通常有哪些变化？

**实验1**
1. 什么是 SIMD 向量化？它与普通循环执行有什么区别？
2. 自动向量化与 OpenMP 并行化分别属于哪一类并行？
3. 在汇编代码中，哪些特征可以说明程序已经发生向量化？

**实验2**
1. 为什么 AoS（Array of Structs）访问可能不利于向量化？
2. SoA（Struct of Arrays）为什么更容易形成连续访存？
3. 在汇编中，连续访存通常会表现出哪些指令特征？

**实验3**
1. 为什么输入数据分布会影响 PGO 优化效果？
2. PGO 为什么可以改善分支预测或代码布局？
3. 如果训练数据与真实运行数据差异很大，PGO 可能产生什么问题？

**实验4**
1. Autotuner 的基本思想是什么？
2. 为什么自动调优有时只带来很小的性能提升？
3. 为什么在不同机器上 Autotuner 的结果可能不同？

**实验5**
1. 什么是 Loop Fusion？为什么循环合并可能减少内存访问开销？
2. 什么是 Strength Reduction？为什么用地址递推可以替代乘法索引？
3. 为什么这些循环优化通常在较高优化等级（如 `-O2`）才会出现？

---

> **提示**：如果运行过程中发现某个实验的输出与预期不符（如 checksum 不一致），请检查是否在容器内执行。所有命令必须在 `linux/arm64` Docker 容器中使用 BiSheng Clang 编译，才能与实验要求的环境一致。
