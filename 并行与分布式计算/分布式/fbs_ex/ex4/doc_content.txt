《并行与分布式计算》实验报告年级、专业、班级姓名实验题目实验四 OpenMP与MPI混合编程实验实验时间 实验地点实验成绩 实验性质□验证性  □设计性  √综合性教师评价：□算法/实验过程正确；   □源程序/实验内容提交     □程序结构/实验步骤合理；□实验结果正确；        □语法、语义正确；        □报告规范；           其他：                                          评价教师签名：一、实验目的理解OpenMP和MPI的并行思想，掌握两者在并行方式上的不同之处；掌握OpenMP与MPI混合编程的基本思想和主要编程方法；使用OpenMP与MPI混合编程实现稠密矩阵向量乘法的并行化。二、实验项目内容本次实验内容为OpenMP与MPI混合编程实验，实验环境为华为鲲鹏云服务器，具体实验内容如下：1. 任务说明稠密矩阵向量乘法（DMV， Dense Matrix Vector）是线性代数中的一个常见操作。其可以用公式表达为：	其中是形状为的实数矩阵，是一个维向量。把从维向量映射到维向量空间。结果的具体计算表达如下：	稠密矩阵向量乘法是可并行化的，根据不同的并行化方法，又可以分为按行并行和按列并行。按行并行将矩阵划分为多个行块，每一个行块与向量做点乘操作，得到结果。将的结果合并起来，则可以得到最终的结果。按列并行将矩阵划分为多个列块，每一个列块与向量对应的部分做点乘操作，得到结果。对进行加和，即，则可以得到最终的结果。本次实验将使用OpenMP与MPI混合编程分别实现上述两种并行方法。2. 并行算法流程0号进程生成数据，分发相应数据给各进程。各进程使用多线程计算分配的数据。各进程将计算结果传回0号进程0号进程对计算结果进行处理，输出最后的结果。提示：①可能使用到的函数：MPI_Wtime(), MPI_Bcast(),MPI_Scatter(),MPI_Gather(),MPI_Reduce()②按列并行可以先对矩阵进行转置，方便进行数据分发。3. 任务要求生成数据的代码已给出，请在此基础上分别实现两种DMV并行方法;void getData(int*& A, int*& x, int& m, int& n) {    m = 7680, n = 42000;    cout << "shape: " << m << " * " << n << endl;    A = new int[m * n];    x = new int[n];    for (int i = 0; i < m; i++) {        for (int j = 0; j < n; j++) {            if (j <= i)                A[i * n + j] = j + 1;            else                A[i * n + j] = 0;        }    }    for (int i = 0; i < n; i++) x[i] = 1;}在MPI主机配置文件中将每台服务器分配的进程数设定为1;Openmp使用的线程数设置为2;需要在控制台中输出的内容：①使用到的节点名称；②结果向量的最后一个元素的值；③并行程序的运行时间（使用MPI_Wtime()获取时间）；控制台输出的结果示例如下：为了分析并行算法，需要在相同数据量下多次执行程序。请在分别在以下条件下运行程序，记录运行时间。① 进程数=1，不使用openmp② 进程数=1，2，3，4，5，6，使用openmp请在实验报告中记录重要的实验配置、过程、代码和结果。4. 分析结果在使用openmp的情况下，分别对两个并行方法绘制以线程数为横坐标，运行时间为纵坐标的折线图。请分析并行程序运行时间变化的原因。三、实验过程或算法（源程序）四、实验结果及分析和（或）源程序调试过程