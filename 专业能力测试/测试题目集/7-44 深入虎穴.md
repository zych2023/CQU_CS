---
tags:
---
# 题目
著名的王牌间谍 007 需要执行一次任务，获取敌方的机密情报。已知情报藏在一个地下迷宫里，迷宫只有一个入口，里面有很多条通路，每条路通向一扇门。每一扇门背后或者是一个房间，或者又有很多条路，同样是每条路通向一扇门…… 他的手里有一张表格，是其他间谍帮他收集到的情报，他们记下了每扇门的编号，以及这扇门背后的每一条通路所到达的门的编号。007 发现不存在两条路通向同一扇门。

内线告诉他，情报就藏在迷宫的最深处。但是这个迷宫太大了，他需要你的帮助 —— 请编程帮他找出距离入口最远的那扇门。

### 输入格式：

输入首先在一行中给出正整数 N（<105），是门的数量。最后 N 行，第 i 行（1≤i≤N）按以下格式描述编号为 i 的那扇门背后能通向的门：

```
K D[1] D[2] ... D[K]
```

其中 `K` 是通道的数量，其后是每扇门的编号。

### 输出格式：

在一行中输出距离入口最远的那扇门的编号。题目保证这样的结果是唯一的。

### 输入样例：

```in
13
3 2 3 4
2 5 6
1 7
1 8
1 9
0
2 11 10
1 13
0
0
1 12
0
0
```

### 输出样例：

```out
12
```
# 解答
```cpp
#include <bits/stdc++.h>
using namespace std;

// 定义pair类型别名（当前代码未使用，保留原定义）
#define pii pair<int, int>

const int MAX_NODE = 100001; // 最大节点数，便于理解数组大小

int N;                  // 节点总数
int K, D;               // 临时变量，用于输入边
int one;                // 存储队列中弹出的当前节点
int vis[MAX_NODE] = {0};// 访问标记数组：0-未访问，1-已访问
vector<int> Edge[MAX_NODE]; // 邻接表：存储每个节点的相邻节点
queue<int> Q;           // BFS遍历使用的队列

int main() {
    // 输入节点总数
    cin >> N;
    
    // 读取每个节点的相邻节点，构建无向图
    for (int i = 1; i <= N; ++i) {
        cin >> K;                  // 第i个节点的相邻节点数量
        while (K--) {
            cin >> D;              // 相邻节点的编号
            Edge[D].push_back(i);  // 无向图：D与i互相添加为相邻节点
            Edge[i].push_back(D);
        }
    }
    
    // BFS初始化：从节点1开始遍历
    Q.push(1);     // 节点1入队
    vis[1] = 1;    // 标记节点1为已访问
    
    // BFS遍历过程
    while (!Q.empty()) {
        one = Q.front();  // 取出队首节点
        Q.pop();          // 队首节点出队
        
        // 遍历当前节点的所有相邻节点
        for (auto v : Edge[one]) {
            if (vis[v]) continue;  // 已访问的节点跳过
            vis[v] = 1;            // 标记为已访问
            Q.push(v);             // 入队，等待后续遍历
        }
    }
    
    // 输出最后一个出队的节点（BFS遍历的最后一个节点）
    cout << one;
    
    return 0;
}
```